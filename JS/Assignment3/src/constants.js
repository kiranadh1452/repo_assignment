const HIGH_RADIUS = 20; //highest possible radius
const LOW_RADIUS = 10; //lowest possible radius

const LOW_SPEED = 2; //lowest speed of the ball
const HIGH_SPEED = 3; // highest speed of the ball

const boundaryWidth = 1500;
const boundaryHeight = 750;
